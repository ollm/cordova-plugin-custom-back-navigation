function emptyCallback() {

}

var CustomBackNavigation = {
	register: emptyCallback,
	nextClosesApp: emptyCallback,
	pointerEvents: emptyCallback,
	useTouchend: emptyCallback,
	simulateTouchend: emptyCallback,
};

module.exports = CustomBackNavigation;